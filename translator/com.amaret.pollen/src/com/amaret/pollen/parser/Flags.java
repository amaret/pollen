package com.amaret.pollen.parser;

public enum Flags {
	CLASS, COMPOSITION, CONST, CONSTRUCTOR, ENUM, ERR, FIELD,
	FCNREF, HOST, LOG, META, TYPE_META_ARG, MODULE, NEW, OUT,
	PROTOCOL, PROTOCOL_MEMBER, PUBLIC,
	VOLATILE, VOID_FCN, VOID_INSTANCE
}
