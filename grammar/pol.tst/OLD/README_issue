What are all the declaration cases?
Verify that they are not ambiguous

protocol member (of a module)
     QName Qname delim
     MyProtocol pro

function reference
     Handler.h f              // no parameters
     Handler.h(T1, T2)  f     // parameters

data member
     uint8 i
     MyClass c            // c initialized to null
     MyClass c()          // c initialized to constructor with no parameters
     MyClass c = new MyClass(p)
     ..or..
     Myclass new c()
     Myclass new c(p)
     ..or..
     new Myclass c()
     new Myclass c(p)


array data member

function declaration
     fcn()
     T1 fcn1()
     (T1, T2) fcn2()
     fcn2(T1 t, T2 s)
      
function definition
     fcn(){..}
     T1 fcn1(){..}
     (T1, T2) fcn2(){..}
     fcn2(T1 t, T2 s){..}


        value{Queue} q
        Queue q = new Queue 

        Queue q() {
        }

        (uint8, bool) myfcn() {
            return (8, true)
        }
    
        pollen.run() {
       
          
    
        }   
}
